# >>> pip install virtualenv [for use virtual environment]
# >>> virtualenv env [for create virtual environment]
# >>> source env/bin/activate [for linux user, for activate this virtual environment]
# >>> .\env\Scripts\activate [for windows powershell user, for activate this virtual environment]
# >>> deactivate [ for deactivate virtual environment]
# >>> pip freeze [for see all of the pkg installed]
# >>> pip freeze > requirements.txt [for add all the list of pkg on requirements text file]
# >>> pip install -r ./requirements.txt [for install all the pkg with this command]


import flask
import pandas as pd